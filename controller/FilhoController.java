package controller;

import model.Filho;

public class FilhoController{
	
/*
    public boolean validarDados(Filho filho) {
         if(filho.getNome_filho().equals("") || filho.getCpf().equals("")){   
		return false;
	}
         // ADCIONAR MAIS REGRAS DE NEGÓCIO
         else if(filho.getIdade_filho() < 2){
             return false;
         }
	else {
		return true;
	}
    }
*/    
}
